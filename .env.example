# .env.example — TEMPLATE, safe to commit to GitHub.
# Copy to ".env" and fill in your real key if you later wire up a backend
# or bundler that reads it. For the static plotmate.html app as-is, the
# key actually used is the one in config.js (copy config.example.js).

GEMINI_API_KEY=PASTE_YOUR_GEMINI_API_KEY_HERE
